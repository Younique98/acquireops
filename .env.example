# Postgres connection - EITHER a single connection string (what Vercel
# Postgres/Neon/Supabase give you) OR the discrete fields below, not both.

# Option A: connection string (used automatically if set)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Option B: discrete fields (local dev)
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=5432
DATABASE_NAME=

# Basic Auth - REQUIRED before any real deployment. This app holds real
# personal financial data. Without these set, the app is unauthenticated.
ADMIN_USERNAME=
ADMIN_PASSWORD=
